'use strict';

const { RequestError } = require('@florajs/errors');

/**
 * Parse "aggregate" options.
 */
module.exports = function aggregateParser() {
    throw new RequestError('aggregate is not implemented yet');
};
